ApiVersion: v1
kind: Secret
metadata:
  name: {{ template "travelling-deliveryman.fullname" . }}-secrets
  labels:
    app: {{ template "travelling-deliveryman.fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
type: Opaque
data:
  redis-password: "abcd0123"
